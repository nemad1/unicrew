version: '3.8'
services:
  openwa-api:
    image: ghcr.io/rmyndharis/openwa:latest
    restart: unless-stopped
    ports:
      - "2785:2785"
    environment:
      - API_KEY=unicrew-239snaoanWef-asdwnt-123asdkajnsdiwe
      - ENGINE_TYPE=whatsapp-web.js
      - STORAGE_TYPE=local
      - DATABASE_TYPE=sqlite
    volumes:
      - openwa-data:/app/data

volumes:
  openwa-data:
